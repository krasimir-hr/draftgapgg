// Shared card surfaces, kept in one place so the home day-card headers and the
// League page hero header stay visually identical.

// The side-rail / standings card surface — flat (shadcn-neutral aesthetic).
export const RAIL_SURFACE = 'var(--surface)';

// The side-rail leaderboard's featured (#1) row — one step up from the rail
// surface so it reads as a highlighted panel.
export const RAIL_FEATURED = 'var(--surface-sub)';
